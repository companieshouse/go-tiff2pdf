//extern tmsize_t ReadProc(thandle_t fd, void* buf, tmsize_t size);

#include "cgo.h"
#include "tif_golang.c"

//static tmsize_t ReadProc(thandle_t fd, void* buf, tmsize_t size) {
  //xreadproc();
  //return size;
//}
